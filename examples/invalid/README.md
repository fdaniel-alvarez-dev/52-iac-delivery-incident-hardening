# Invalid examples (intentional)

These inputs are designed to fail validation, so you can demonstrate the guardrails.

Run:
```bash
PYTHONPATH=src .venv/bin/python -m portfolio_proof validate --examples examples/invalid
```

